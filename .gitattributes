# Скрипты и конфиги для установки на Linux/РЕД ОС всегда должны иметь LF,
# даже если репозиторий выкачан на Windows с core.autocrlf=true — иначе
# CRLF в шебанге (#!/usr/bin/env bash\r) ломает запуск на целевой машине.
deploy/redos8/* text eol=lf

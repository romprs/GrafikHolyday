"""Периодический запуск синхронизации.

Рекомендуемый подход — внешний планировщик (cron / k8s CronJob), дёргающий
POST /admin/sync/run: избегает риска дублирующихся запусков, если бэкенд
когда-нибудь будет работать в нескольких репликах. In-process APScheduler
годится только для локальной разработки и здесь сознательно не подключён,
чтобы не тащить лишнюю зависимость и не создавать два конкурирующих
источника правды о расписании синка.

Пример cron-записи для локальной разработки (раз в час):
    0 * * * * curl -X POST http://localhost:8000/admin/sync/run \\
        -H "X-Dev-User-Id: <hr_admin_user_id>"
"""

# Программа планирования отпусков сотрудников

## Статус: Phases 0–5 готовы, реальные интеграции подключены, Phase 6 (доводка) в процессе

Актуальность проверена 24.09.2026: [отчёт сверки со стендом](docs/deployment-comparison-2026-09-24.md).
Свежая сборка фронтенда совпадает с сервером побайтно, OpenAPI — полностью.
251 backend-тест проходит. Совпадение внутренних файлов бэкенда и состояние
миграций на сервере по HTTP не установлены. Описания этапов ниже сохраняют
историю: старые числа тестов, адреса архивов и результаты проверок относятся
к моменту соответствующего этапа.

Текущая рабочая папка: `E:\Claude\GrafikHolyday`, remote `origin`:
`https://github.com/romprs/GrafikHolyday.git`, ветка
`claude/vacation-planning-program-4tcouk`. Прежняя схема доставки через
`/workspace/grafikholyday` и remote `local-work` к этой рабочей среде не относится.
Проверка 24.09.2026 не включала push или повторное развёртывание.

## Контекст (не изменился)

Компании нужен веб-инструмент для подачи и согласования отпусков, который решает несколько проблем одновременно: сотрудники должны выбирать период визуально и не иметь возможности попасть в дни, когда отдел занят учёбой/сборами; руководители должны видеть нагрузку по отделам, чтобы не допускать одновременного ухода в отпуск большей части команды; HR должен иметь возможность гибко включать/выключать ограничения и учитывать сотрудников со льготами, на которых эти ограничения не действуют. Структура компании (отделы, управления, сотрудники, льготы) — синхронизация из внешней корпоративной системы (реальный REST/OData-контракт подключён, см. ниже).

**Стек:** Python (FastAPI) + PostgreSQL (SQLAlchemy + Alembic), React + TypeScript (Vite).
На стенде `https://192.168.0.10/` запрос без авторизации получает Kerberos/SPNEGO
challenge, dev-вход отключён. Успешный доменный вход и конкретный `KERBEROS_MODE`
текущей сверкой не проверялись. Для локальной разработки доступен `AUTH_PROVIDER=dev`.
OIDC остаётся заглушкой. Реализованы оба режима Kerberos (nginx/python)
и аварийный вход HR-admin; настройка описана в `deploy/redos8/README.md`.

**Ключевые решения:**
- % загруженности отдела = доля сотрудников юнита в отпуске в конкретный день, пороги (жёлтый/красный) настраиваются через `restriction_settings` (`department_load_thresholds`)
- Дни отпуска — календарные за вычетом праздников из `app/core/holidays.py`, включая 31 декабря по требованиям проекта; дата окончания периода автоматически не сдвигается.
- Согласование — руководителем по иерархии, назначенным согласующим (`is_approver`) или делегатом. Самосогласование запрещено, кроме руководителя корневого подразделения; роль HR-admin сама по себе не даёт права согласовать чужой отдел.
- Все ограничения управляемые через настройки, с полным исключением сотрудников с `has_benefits=true` (кроме правила пересечения своих же периодов — оно не про льготы)
- Оргструктура — произвольная глубина вложенности, импорт по REST/OData из внешней системы, read-only в приложении для обычных ролей, CRUD доступен `hr_admin`
- Планирование ведётся на конкретный **плановый год** (`restriction_settings.planning_year`), влияет на баланс, пикер и дашборд загруженности
- **Каждая внешняя интеграция независима**: своя запись в `restriction_settings`, свой `sync_runs.kind`, свой admin-роутер. Синхронизация оргструктуры/сотрудников (drx, `GetDepartments`/`GetEmployeers`) и синхронизация дней отпуска/льгот (другая система, `hrm_new3`, `GetVacationDaysCount` по табельному номеру) — два independent процесса, специально спроектированные так, чтобы не затирать данные друг друга (`has_benefits: bool | None` — `None` значит "источник не знает, не трогать")

## Модель данных (PostgreSQL) — как есть сейчас

- **`org_units`** (id, parent_id→org_units, name, unit_kind, head_user_id→users, is_active) — adjacency list, `WITH RECURSIVE` для обхода. `use_alter=True` на `head_user_id` (циклическая FK с `users`). CRUD доступен `hr_admin` (создание/редактирование/деактивация).
- **`users`** (id, email, full_name, org_unit_id, has_benefits, is_active, employee_code, last_synced_at, hire_date, termination_date, is_approver). `employee_code` (табельный номер) — уникальный, nullable, ключ связи с внешними системами (учебные планы, дни отпуска). Роль руководителя вычисляется (`head_user_id` какого-то `org_unit` == этот user), не хранится. CRUD доступен `hr_admin`.
- **`user_roles`** (user_id, role='hr_admin') — единственная локальная (не синковая) таблица ролей; управляется через страницу «Роли».
- **`leave_delegations`** (id, scope[user/org_unit], delegate_user_id, target_user_id, target_org_unit_id, is_active, created_by, created_at, revoked_at) — делегирование права согласования: либо "сотрудник за сотрудника" (`scope=user`), либо "сотрудник за всё подразделение" (`scope=org_unit`, каскадом на все вложенные юниты). Управляется через страницу «Делегирование».
- **`leave_types`** (id, code, name_ru, is_active) — сид `vacation`.
- **`leave_requests`** (id, user_id, leave_type_id, date_from, date_to, comment, status, reviewer_id, review_comment, submitted_at, reviewed_at, cancelled_at, cancelled_by, submission_id, bonus_requested).
  Статусы: `draft` → `pending_approval` → `approved`/`rejected`, либо `cancelled` из pending/approved.
  **`draft` — активно используемый статус**: период сохраняется в БД сразу при добавлении в форме, а не хранится в состоянии компонента — переживает переключение вкладок/перезагрузку. Отправка на согласование (`submit_drafts`) переводит все черновики года разом в `pending_approval` одной `submission_id`-группой, но **только если их суммарная длительность точно равна остатку баланса**. Согласование/отклонение/отмена — атомарно по всей группе (`submission_id`), не по отдельным периодам.
- **`leave_balances`** (user_id, year, accrued_days, carried_over_days, updated_by) — `used`/`remaining` вычисляются на лету. Остаток для новой заявки/черновика учитывает `approved + pending_approval + draft` (чтобы нельзя было "перекредитовать" баланс). `accrued_days` обновляется синхронизацией дней отпуска, `carried_over_days` — никогда автоматически (только вручную HR).
- **`blocked_periods`** (id, date_from, date_to, reason, scope[global/org_unit/user], org_unit_id, user_id, is_active, created_by) — наследование по дереву org_unit. Источник для `scope=org_unit` может приходить из внешней системы учебных планов (см. `study_periods_source`).
- **`restriction_settings`** (key PK, enabled, params jsonb, description) — сиды: `min_leave_duration`, `blocked_period_enforcement`, `department_load_thresholds`, `leave_balance_limit`, `own_overlap_check`, `planning_year`, `vacation_bonus`, `auth_configuration`, `external_source_connection` (оргструктура+сотрудники, drx), `study_periods_source` (учебные планы 1С), `vacation_days_source` (дни отпуска+льготы, hrm_new3).
- **`external_id_mappings`**, **`sync_runs`** (с дискриминатором `kind`: `org_directory`/`study_periods`/`vacation_days`), **`sync_change_log`** — общий движок синхронизации + журнал изменений для всех трёх интеграций.
- **`audit_log`** (entity_type, entity_id, action, performed_by, reason, before_state, after_state) — пишется при HR admin-override и при HR CRUD над `org_units`/`users`.

## Структура бэкенда (FastAPI) — как есть сейчас

```
app/
  main.py, config.py, database.py, dependencies.py
  auth/            interface.py (AuthProvider ABC), dev_provider.py,
                    kerberos_provider.py (боевая схема — Kerberos/SPNEGO,
                    см. Phase 6.11: resolve_login сопоставляет логин с
                    User.email, KerberosGSSAPIAuthProvider — режим
                    KERBEROS_MODE=python; login_from_trusted_header —
                    режим KERBEROS_MODE=nginx). oidc_provider.py — заглушка
                    под OIDC, больше не актуальна (не тот протокол),
                    оставлена нетронутой на случай, если пригодится позже.
  models/          org_unit, user, user_role, leave_delegation, leave_type,
                    leave_request, leave_balance, blocked_period,
                    restriction_settings, external_id_mapping, sync (SyncRun,
                    SyncChangeLog, KIND_*), audit_log
  schemas/         зеркалит models/
  routers/         org_units (+CRUD), users, admin_users (CRUD сотрудников,
                    employee-code), leave_requests, leave_balances,
                    blocked_periods, calendar, org_load, restriction_settings,
                    admin_sync (оргструктура+сотрудники), admin_study_periods,
                    admin_vacation_days, delegations, audit, roles
  services/
    permissions.py                  — резолвинг роли (employee/manager/hr_admin)
    leave_request_service.py        — create_draft/list_drafts/delete_draft/
                                       submit_drafts, list_own, list_team_leave, cancel
    approval_service.py             — approve/reject по submission_id, admin-override
                                       → audit_log, учитывает делегирование
    delegation_service.py           — create/revoke/list, резолвинг "кто может
                                       согласовывать за X" (прямой руководитель
                                       + активные делегации user/org_unit-scope
                                       с каскадом по дереву)
    leave_balance_service.py        — get_summary, get_remaining_for_new_request
    org_unit_service.py             — recursive CTE: descendants(id), ancestors(id)
    org_load_service.py             — get_org_load (агрегат по дням), get_org_leave_detail
                                       (сырые employees+leaves для интерактивного дашборда)
    blocked_period_service.py       — CRUD + эффективные блокировки
    restriction_settings_service.py — включая get_planning_year()
    user_admin_service.py           — HR CRUD над users/org_units, employee_code
    audit_service.py
    sync_service.py                 — общий 3-pass upsert (org_units → users →
                                       head_user_id) через external_id_mappings,
                                       kind=org_directory
    study_period_sync_service.py    — синхронизация недоступных периодов,
                                       kind=study_periods
    vacation_days_sync_service.py   — синхронизация accrued_days + has_benefits
                                       по employee_code, kind=vacation_days,
                                       НЕ трогает carried_over_days
    validation/engine.py            — прогоняет включённые правила, льготники
                                       пропускают exemptable-правила
    validation/min_duration_rule.py
    validation/blocked_period_rule.py
    validation/leave_balance_rule.py
    validation/own_overlap_rule.py  — не exemptable (физическое ограничение)
  integrations/    org_directory.py (реальный клиент: GetDepartments +
                    GetEmployeers, Basic Auth), study_periods.py, vacation_days.py
                    (реальный клиент: GetVacationDaysCount по tabnum)
  sync/            interface.py (ExternalDirectoryClient ABC), dto.py
                    (ExternalOrgUnitDTO/ExternalUserDTO), fake_client.py (dev),
                    scheduler.py
alembic/versions/... (полная цепочка миграций Phase 0 → делегирование →
                    employee_code → sync kind → vacation_days_source, проверена
                    "upgrade head" с нуля на реальном Postgres 16)
tests/unit/ — 251 тест (проверка 24.09.2026), pytest + SQLite in-memory (плюс пара тестов
                    на FastAPI TestClient — auth_challenge_error)
```

**Дашборд загруженности отдела** — единая таблица (`OrgLoadDashboardPage`): месяцы по строкам, дни по столбцам, заполнены только непустые ячейки (есть кто-то в отпуске), клик по ячейке показывает список отпускников. Фильтр по роли (руководитель/специалист/все), поиск по фамилии (с 4 букв), произвольный выбор сотрудников для точечного анализа пересечений, согласование прямо с дашборда. Бэкенд отдаёт сырые данные (`get_org_leave_detail`) — вся агрегация и раскраска (`green`/`yellow`/`red` по порогам) считается на фронте.

**Блокировка правок после согласования** — `approve/reject` только из `pending_approval`, атомарно по всей группе `submission_id`; после `approved` доступна только отмена (руководителем/делегатом или самим сотрудником — по правилам). Исключение — `PATCH /leave-requests/{id}/admin-override` (`hr_admin`, обязательный `reason`, пишет в `audit_log`).

**Делегирование** — два независимых сценария: точечное "сотрудник за сотрудника" и "сотрудник за всё подразделение" (каскадом на вложенные юниты — чтобы не заводить делегацию на каждый отдел вручную). Учитывается наравне с прямым руководством во всех местах, где резолвится "кто может согласовать".

## Структура фронтенда (React + TS) — как есть сейчас

- `api/` — типизированный клиент (`apiFetch`, `ApiError` со структурой `{code, message_ru, params}`)
- `auth/AuthContext` — dev-режим (выбор пользователя из списка), `X-Dev-User-Id` заголовок. Хедер (ФИО + роль + кнопка «Выйти» в одну строку, «Выйти» справа) общий для всех вкладок.
- `pages/`: RequestFormPage (план отпуска — черновики + отправка), MyRequestsPage (календарь + отмена целиком), ApprovalQueuePage, BlockedPeriodsPage, OrgDirectoryPage (объединяет бывшие OrgUnitsPage/EmployeesPage/RolesPage — таблица подразделений+сотрудников: сворачивание по узлам, фильтры ФИО/роль/подразделение с автораскрытием пути, инлайн-редактирование сотрудника HR admin'ом — подразделение/льготы/таб.номер/баланс/роль, руководитель всегда первой строкой в подразделении, деактивированные подразделения видны HR приглушённым цветом с кнопкой реактивации), OrgLoadDashboardPage (вкладка «Отпуска подразделений», бывшая «Загруженность отделов» — список сотрудников с поиском слева, график месяцы×дни по центру, описание дня по клику справа), IntegrationsSettingsPage (настройки всех трёх внешних источников: оргструктура/сотрудники, учебные планы, дни отпуска+льготы — enable/URL/auth, ручной запуск, история запусков, плюс загрузка оргструктуры/сотрудников файлом), RestrictionSettingsPage, AllRequestsPage, AuditLogPage, DelegationsPage (создание/отзыв делегаций user- и org_unit-scope; делегат для руководителя ограничен его веткой подчинения, поиск по фамилии). TeamCalendarPage удалена — дублировала OrgLoadDashboardPage.
- Поиск по фамилии (Delegations, OrgLoadDashboard) — helper `surname()` берёт **первое** слово ФИО, т.к. в приложении ФИО хранится как «Фамилия Имя Отчество» (не Western-порядок) — эта деталь один раз уже была багом (брали последнее слово), исправлено.
- `components/DateRangePicker` — обёртка над `react-day-picker` v9, показывает **все 12 месяцев планового года одновременно** (`numberOfMonths={12}`, `disableNavigation`). `disabled` = недоступные периоды + уже добавленные в план черновики. Недоступные дни — серым фоном (было зачёркивание); выходные/праздники — светло-красным (`src/holidays.ts`, фиксированный список по ст. 112 ТК РФ, без ежегодного переноса выходных — на 2027 год постановление ещё не публиковалось).
- `holidays.ts` — общий helper `isNonWorkingDay()` (выходной ИЛИ праздник), используется и в DateRangePicker, и в OrgLoadDashboardPage.
- Страница целиком принудительно светлая (`<meta name="color-scheme" content="light">` + `index.css`) — приложение не проектировалось под тёмную тему, а без этого браузер/ОС в тёмном режиме сам перекрашивал нативные элементы и терял контраст.
- BlockedPeriodsPage — фильтр по плановому году (по умолчанию текущий) поверх общего списка недоступных периодов из 1С — без него список из тысяч записей на все года бесполезен.
- OrgDirectoryPage — блок «Без подразделения» теперь сворачивается/разворачивается как обычное подразделение (участвует в «Развернуть всё»/«Свернуть всё»), раньше всегда показывал список целиком.
- OrgLoadDashboardPage — панель деталей дня (клик по ячейке) теперь обычная третья колонка справа от графика (`flex:"0 0 260px"`, sticky), а не оверлей: две прошлые попытки (`position:fixed` в углу, модалка с подложкой) либо сжимали график, либо перекрывали его целиком. Исправлены две реальные причины, из-за которых колонка раньше не помещалась и уезжала под график: у колонки графика был `flex-grow:1` (жадно съедала всё свободное место вместо своих ~1200px), а у списка сотрудников слева не было `overflowX:hidden` (сайдбар раздувался на 48px сверх заданных 300). Страница расширена до `maxWidth:2000` — график из 31 колонки помещается без горизонтальной прокрутки на обычном широком экране. Дата в деталях дня — формат дд.мм.гггг, закрытие — крестиком в углу панели. Выходные/праздники подсвечены и в самом графике (light-red, если день пустой).
- Длинные ФИО нигде не переносятся на 2 строки — `whiteSpace:nowrap` + `text-overflow:ellipsis` + `title` с полным именем (список сотрудников слева на OrgLoadDashboard, панель деталей дня).
- Серверное состояние — TanStack Query, инвалидация кэша между заявками/балансом/календарём/дашбордом. `AuthContext.loginAs()`/`logout()` теперь вызывают `queryClient.clear()` — раньше при смене dev-пользователя в кэше на миг оставались данные предыдущего (в т.ч. недоступные ему), что давало спонтанные 403 и "пустые" графики сразу после релогина.
- Вся текстовая часть на русском (без i18n-библиотеки — строки хардкожены по-русски прямо в компонентах)

## Поэтапный план сборки — статус

1. **Phase 0 — Каркас и авторизация-заглушка.** ✅ Готово.
2. **Phase 1 — CRUD заявок + согласование.** ✅ Готово (позже переработано в draft-flow, см. Phase 5.2).
3. **Phase 2 — Календарь, графический пикер, блокировки.** ✅ Готово.
4. **Phase 3 — Синхронизация оргструктуры + дашборд загруженности.** ✅ Готово (дашборд позже полностью переделан; клиент позже заменён на реальный, см. Phase 6).
5. **Phase 4 — Управляемые ограничения, льготы, аудит.** ✅ Готово.
6. **Phase 5 — Обратная связь по демо + полировка.** ✅ Готово:
   - Баланс на форме заявки, лимит по остатку, несколько периодов в заявке, плановый год как настройка, страница «Сотрудники», страница «Роли»
   - Дашборд загруженности переделан в единую таблицу месяцы×дни с фильтрами/поиском/произвольным выбором сотрудников, согласование прямо с дашборда
   - Черновики периодов (`draft`), отправка/согласование/отмена атомарно по группе `submission_id`
   - Недоступные периоды из внешнего источника (учебные планы 1С), с областью «Сотрудник»
   - Бонус к отпуску (порог "от 14 дней", один период на план)
   - Офлайн-развёртывание на РЕД ОС 8 (`deploy/redos8`: install.sh, nginx, systemd-unit, сборка бандла)
7. **Phase 6 — Реальные интеграции + управление структурой.** 🔶 В процессе:
   - ✅ 6.1 Роли: делегирование заявок (user + org_unit scope с каскадом), видимость вкладок/оргструктуры по роли
   - ✅ 6.2 HR: CRUD подразделений и сотрудников прямо в приложении
   - ✅ 6.3 Реальный клиент источника оргструктуры (`GetDepartments`, drx, Basic Auth) вместо `fake_client`
   - ✅ 6.4 Реальный клиент синхронизации сотрудников (`GetEmployeers`, тот же источник) — email/employee_code/org_unit по логину/tabnum/podrid, только числовые табельные номера, сотрудники без логина пока пропускаются
   - ✅ 6.5 Отдельная синхронизация дней отпуска и льготы (`GetVacationDaysCount`, другая система hrm_new3, запрос по каждому tabnum) — независимая настройка/роутер/история запусков, не пересекается с 6.4 по владению `has_benefits`. Запрашивает внешний источник строго по одному сотруднику за раз (последовательно, без параллелизма) — на ~3900 реальных сотрудниках это может быть заметно долгим синхронным HTTP-запросом от кнопки «Синхронизировать сейчас»; если станет проблемой на реальных объёмах — сделать синк фоновым и/или распараллелить запросы (не сделано, пользователь пока не просил).
   - ✅ 6.6 Фикс сопоставления сотрудников с подразделениями при раздельной загрузке (sync_service резолвил ссылки только по текущей выгрузке, не заглядывая в уже существующие external_id_mappings) + скрипт чистки `reset_org_directory_sync.py`
   - ✅ 6.7 Загрузка оргструктуры/сотрудников файлом (без ожидания рабочего HTTP-источника) — `POST /admin/sync/import`, тот же external_system, что у HTTP-клиента
   - ✅ 6.8 Единая вкладка «Оргструктура и сотрудники» (см. OrgDirectoryPage выше) вместо трёх разрозненных страниц
   - ✅ 6.9 Делегирование: делегат для руководителя ограничен его веткой подчинения + поиск по фамилии
   - ✅ 6.10 Убрана вкладка «Календарь отдела» (дублировала «Загруженность отделов»); та переименована в «Отпуска подразделений» и переверстана (сотрудники слева, график по центру, детали дня справа)
   - ✅ 6.11 Реальная аутентификация — **Kerberos/SPNEGO** через AD-домен `corp.amurgpz.ru`, логин = локальная часть Kerberos-принципала/email (сопоставляется с `User.email` через `ILIKE 'login@%'`, `app/auth/kerberos_provider.resolve_login`). Реализованы **оба варианта** проверки SPNEGO-тикета, переключаются `KERBEROS_MODE` без правки кода:
     - `KERBEROS_MODE=nginx` — тикет проверяет nginx (модуль с директивами `auth_gssapi_*`, образец — `deploy/redos8/nginx-gssapi.conf.example`), бэкенд читает уже подтверждённый логин из заголовка `KERBEROS_TRUSTED_HEADER` (по умолчанию `X-Remote-User`) — `app/auth/kerberos_provider.login_from_trusted_header`. Бэкенд Kerberos не касается вообще (ни keytab, ни python-gssapi).
     - `KERBEROS_MODE=python` — тикет проверяет сам бэкенд через `python-gssapi` (`app/auth/kerberos_provider.KerberosGSSAPIAuthProvider`, ленивый импорт gssapi + keytab из `KERBEROS_KEYTAB_PATH`, SPN `HTTP/<KERBEROS_SERVER_HOSTNAME>@<KERBEROS_REALM>`); при отсутствии Authorization-заголовка отдаёт `401 + WWW-Authenticate: Negotiate` (новый `AuthChallengeError` в `app/core/exceptions.py`) — без этого браузер не станет присылать SPNEGO-токен. Провалидировано на реальном uvicorn (fail-fast на старте при отсутствии `gssapi`/незаполненном hostname; корректный 200/403 для nginx-режима с реальным HTTP-запросом).
     - Оба режима покрыты тестами (`tests/unit/test_kerberos_provider.py`, `tests/unit/test_auth_challenge_error.py`).
     - `python-gssapi` — опциональная зависимость (`kerberos` extra в `pyproject.toml`), без готовых manylinux-колёс на PyPI; для офлайн-бандла нужно собрать колесо отдельно (см. `deploy/redos8/README.md`) — `install.sh` подхватывает его из `wheelhouse/`, если найден, иначе просто пропускает (не ошибка).
     - **Открыто**: не решено, какой именно `KERBEROS_MODE` использовать в проде — зависит от того, есть ли на сервере nginx с GSSAPI-модулем (см. «Открытые допущения» ниже), плюс от IT ещё нужны сам keytab, `KERBEROS_SERVER_HOSTNAME` и подтверждение realm.
   - ✅ 6.16 Аварийный вход для hr_admin, независимый от `AUTH_PROVIDER` — запрос пользователя: «если включу Kerberos, не потеряю ли возможность зайти как hr_admin, если что-то сломается». Отдельный общий пароль (`ADMIN_FALLBACK_PASSWORD` в `.env`, пусто = выключено), проверяется в `get_current_user` раньше и независимо от dev/kerberos-логики (`app/auth/admin_fallback.py`): даже с верным паролем пускает только под учётной записью, у которой прямо сейчас есть роль `hr_admin` — знание пароля не даёт войти под кем угодно. Форма «Аварийный вход HR-admin» на экране логина (email+пароль), креды — в `sessionStorage` (не переживают закрытие вкладки, в отличие от dev-выбора пользователя). Попутно исправлен баг: `AuthContext` дёргал `/auth/dev/users` без `try/catch` — в режиме `kerberos` этот эндпоинт всегда 403, и необработанное исключение блокировало всю дальнейшую инициализацию (`setLoading(false)` не вызывался), приложение оставалось на пустом экране навсегда.
   - ✅ 6.18 Аварийный вход починен и для `KERBEROS_MODE=nginx` (пользователь готовит реальный `vp_web.keytab` для боевого сервера в этом режиме и напомнил про резервный вход) — при этом режиме `location /api/` у nginx проверяет тикет безусловно, до бэкенда, поэтому вариант из 6.16 там физически не мог сработать. Решено штатным приёмом nginx: `satisfy any` + `auth_request` на новый internal-эндпоинт бэкенда `GET /auth/admin-fallback/check` (не часть публичного API, только 200/403 для nginx; реальная идентификация всё равно в `get_current_user` на основном запросе). Логика проверки вынесена в переиспользуемую `resolve_admin_fallback()`, дублирования нет. Готовый пример — `nginx-gssapi.conf.example` (путь к keytab в примере — `vp_web.keytab`, как у пользователя). 3 новых теста, итого 196 backend-тестов проходят, `main.py` собирается без циклических импортов.
   - ✅ 6.12 Доводка UI «Отпуска подразделений»/пикера периода: принудительно светлая тема, сворачивание «Без подразделения», подсветка выходных/праздников (`src/holidays.ts`), серый фон вместо зачёркивания у недоступных дней, фильтр по году в недоступных периодах, ФИО без переноса на 2 строки, панель деталей дня — правая колонка вместо оверлея (см. подробности выше)
   - ✅ 6.13 Три бага делегирования/кэша, найденные и исправленные на реальном сценарии (менеджер с делегированным подразделением):
     - вкладка «Новая заявка» пряталась, если у самого делегата уже есть активная заявка на год — даже когда он хочет подать за подопечного без активных заявок. Теперь видна, если нет своей активной заявки ИЛИ есть хотя бы один делегированный подопечный (backend и так проверяет лимит per-subject, не per-actor)
     - `AuthContext` не чистил кэш React Query при логине/логауте — при смене dev-пользователя данные (и 403-ошибки) предыдущего на миг просачивались новому. Добавлен `queryClient.clear()` на каждую смену пользователя
     - деактивированные подразделения были видны и выбираемы в селекте «Отпуска подразделений» (единственное такое место — в «Оргструктуре» они уже были отфильтрованы/помечены)
   - ✅ 6.14 Логирование HTTP-синхронизации недоступных периодов (1С) приведено к тому же виду, что у синка оргструктуры: `StudyPeriodsClient.fetch_raw()` теперь логирует каждый запрос/ответ (URL, табельный номер, HTTP-статус, превью тела при ошибке — раньше не логировал вообще ничего), `study_period_sync_service` — лог начала/итога HTTP-фазы и общий `try/except` с полным traceback вокруг DB-логики (раньше необработанная ошибка обработки падала бы без сохранённого `error_message`).
   - ⬜ 6.15 (отменено) Клиентский сертификат для источника учебных планов — изначально добавил PFX-авторизацию в `StudyPeriodsClient`, но пользователь уточнил: сертификат не для 1С, а для HTTPS самого приложения (см. Phase 6.17). Откачено полностью — `StudyPeriodsClient` снова только Basic-логин/пароль, как раньше. Реальный URL источника (`https://0svb1-1c-01.corp.amurgpz.ru/gppb_pl_anonimouse/ru/hs/Employee/study_plan/821b62a4-938a-11f0-9c17-566f5bd5001e`) остаётся сохранённым через UI — это было верно и не откатывалось. Причина, почему синк с 1С не работает, всё ещё не найдена — логи (Phase 6.14) есть, ждём следующего запуска.
   - ✅ 6.17 HTTPS для nginx через корпоративный `.pfx`-сертификат сервера (сервер в домене, сертификат от доменного CA) — `install.sh TLS_PFX_PATH=... [TLS_PFX_PASSWORD=...]` сам конвертирует `.pfx` в PEM через `openssl pkcs12` (fallback на `-legacy` для старых Windows CA), кладёт в `/opt/vacation-planner/tls/`, ставит SELinux-контекст, выбирает `nginx-vacation-tls.conf` (443 + сертификат, 80 → редирект) вместо обычного HTTP-конфига. Это только шифрование канала — кто именно вошёл, по-прежнему решает `AUTH_PROVIDER`/Kerberos (SSO по доменной учётке входа в ПК, уже реализовано, см. 6.11). Заодно найден и исправлен риск: `build-bundle.ps1`/`.sh` не копировали новый `nginx-vacation-tls.conf` в офлайн-бандл, а `install.sh` теперь требует его наличия всегда — без фикса сборка сломала бы установку вообще всем, не только тем, кто использует TLS.
   - ✅ 6.19 Проверка Kerberos/SPNEGO на реальном стенде (не только тестами) — поднят тестовый AD-домен `TEST.LOCAL` на Samba 4 (роль DC на отдельной ВМ, `SAMBA_INTERNAL` DNS + `dns forwarder` на внешний DNS для сохранения доступа к репозиториям пакетов), тестовая ВМ (та же, что хостит Postgres приложения) введена в домен через `realmd`/`adcli`/`sssd`, выпущен и установлен keytab `HTTP/<hostname>@TEST.LOCAL`. На этой ВМ развёрнут офлайн-бандл (`install.sh`), приложение поднято на реальном nginx+uvicorn. Попутно подтверждён факт (не только предположение из 6.11): **на РЕД ОС 8 нет пакета с модулем `auth_gssapi` для nginx** ни в одном подключённом репозитории (`dnf list --available` — пусто; есть только Apache `mod_auth_gssapi`) — значит `KERBEROS_MODE=nginx` на этой версии ОС недоступен без сборки модуля из исходников, реалистичный выбор для прода — `KERBEROS_MODE=python`. `python-gssapi` в этом режиме поставлен через `pip install gssapi` (компилируется на месте из исходников; портативный Python записывает `clang` компилятором по умолчанию — пришлось явно передать `CC=gcc CXX=g++`, иначе сборка падает с `No such file or directory: 'clang'`, т.к. `clang` в системе не установлен). Сквозной SPNEGO-запрос (`kinit` доменным пользователем + `curl --negotiate` к `/api/users/me`) подтверждён end-to-end: тикет валидируется бэкендом, логин корректно извлекается из принципала и сопоставляется с `User.email`. Отдельно проверено, что аварийный вход hr_admin (Phase 6.16) продолжает работать в `KERBEROS_MODE=python` без какой-либо дополнительной настройки, как и предполагалось (в этом режиме Kerberos целиком проверяет бэкенд, а не nginx, поэтому `satisfy any`-обвязка из 6.18 тут не нужна) — запрос с заголовками `X-Admin-Fallback-*` и без Kerberos-тикета прошёл на том же эндпоинте.
   - ✅ 6.20 Стенд Phase 6.19 доведён до полной имитации прода (HTTPS + Kerberos вместе) — сгенерирован самоподписанный `.pfx` для `test.ru` (реального корпоративного сертификата для тестового домена нет и не нужно) и подан в `install.sh TLS_PFX_PATH=...` теми же средствами, что и для боевого сертификата (Phase 6.17); `install.sh` идемпотентен — повторный запуск на уже установленном сервере просто переключил nginx на HTTPS, ничего не сломав. Проверено на 443: HTTP→HTTPS редирект, сквозной Kerberos через `/api/users/me`, аварийный вход hr_admin — все зелёные; отдельно проверен корректный SPNEGO-челлендж (`401 + WWW-Authenticate: Negotiate`) на запрос без тикета — это то, что заставляет браузер начать Kerberos-согласование сам, без него SSO в браузере не заработал бы даже при полностью рабочем бэкенде. Заодно найдена и исправлена реальная проблема живучести стенда: DNS на клиенте (`test.ru`) был указан на контроллер домена только через ручную правку `/etc/resolv.conf`, а профиль NetworkManager (`nmcli`) всё ещё был настроен на внешний DNS — при перезагрузке `resolv.conf` пересоздался бы с неправильным сервером, и Kerberos/SRV-резолвинг перестал бы работать после первого же ребута стенда. Исправлено через `nmcli connection modify ... ipv4.dns 192.168.0.12 ipv4.dns-search test.local` — теперь настройка переживает перезагрузку. Также подтверждено, что все нужные для стенда сервисы (`nginx`, `vacation-backend`, `postgresql-17` на клиенте; `sssd`; `samba` на DC) включены в автозапуск.
   - ✅ 6.21 Найден и исправлен реальный пробел в браузерном Kerberos-SSO, обнаруженный только на живом стенде (Phase 6.19/6.20): `AuthContext.tsx` (`refreshMe()`) вообще не делал запрос к `/users/me`, если не было ранее сохранённого dev-пользователя или аварийных креды — единственных двух "маркеров входа", которые фронтенд умел проверять. Для чистого Kerberos-сценария ни тот, ни другой никогда не появляется, поэтому браузеру физически не давали шанса ответить на `401 + WWW-Authenticate: Negotiate` тикетом — SSO не работало не из-за DNS/keytab/политик браузера (всё это было настроено верно), а из-за того, что защищённый запрос на фронтенде вообще не отправлялся. Исправлено: `/users/me` теперь запрашивается безусловно при загрузке приложения — для dev/аварийного режима без сохранённых креды поведение не изменилось (тот же 401/403, тот же результат), а для Kerberos это единственный способ дать браузеру провести SPNEGO-согласование. Подтверждено на стенде: `testuser`/`testuser2` (второй — с ролью `hr_admin`, для проверки этой роли через настоящий Kerberos, а не только через аварийный вход) заходят в браузере на `https://test.ru` автоматически, без формы логина. Собран и передан новый офлайн-архив (`vacation-planner-offline-bundle-20260901-215531.tar.gz`, `D:\111`) с этим фиксом; `python-standalone`/`wheelhouse` переиспользованы из предыдущей сборки без изменений (бэкенд в этой правке не менялся). Инструкция по установке (артефакт «Установка и HTTPS») дополнена разделом про `KERBEROS_MODE=python`, сборку `gssapi` (`CC=gcc CXX=g++ pip install gssapi`) и обязательную настройку Negotiate-allowlist в браузере (`AuthServerAllowlist` для Chromium) — без неё валидный Kerberos-тикет на клиенте ничего не даёт, ровно то, на чём споткнулись при живой проверке.
   - ✅ 6.22 Полностью офлайн-установка `gssapi` (для `KERBEROS_MODE=python`, см. 6.19) + найден и исправлен реальный баг, стиравший боевой `.env` при повторном запуске `install.sh`. Колесо `gssapi` (собранное ранее на тестовом сервере) и его зависимость `decorator` (universal wheel с PyPI) положены в `wheelhouse/` бандла — `install.sh` уже умел подхватывать `gssapi-*.whl` оттуда (условие было готово с 6.11, просто нечего было подхватывать), теперь ставится офлайн без `gcc`/`krb5-devel`/интернета на целевом сервере. Проверено: `pip uninstall` + переустановка строго `--no-index --find-links=wheelhouse` на живом сервере — работает.
     **Найденный попутно баг** (реальная причина, почему после переустановки для HTTPS сегодня отвалился весь Kerberos): `build-bundle.ps1`/`.sh` копировали исходники бэкенда через `robocopy`/`tar` без исключения `.env` — локальный dev-`.env` разработчика (только `DATABASE_URL`, без авторизации) молча попадал в каждый собранный архив. При запуске `install.sh` шаг [2/7] (`rsync -a --delete app/ → /opt/vacation-planner/app/`) этим бандл-`.env` **перезаписывал уже настроенный боевой `.env` на сервере** — раньше, чем срабатывала защита «не трогать, если существует» в шаге [4/7], которая к этому моменту просто не видела разницы (файл уже подменён). Живой стенд пострадал от этого при сегодняшней переустановке для HTTPS — Kerberos/аварийный пароль пропали молча, без ошибки при установке. Исправлено в трёх местах: `build-bundle.ps1` (`robocopy /XF .env`) и `build-bundle.sh` (`tar --exclude=.env`) больше не кладут `.env` в бандл вообще; `install.sh` дополнительно сам защищается — снимает копию текущего `.env` до `rsync --exclude=.env` и возвращает её на место после, независимо от того, что лежит в бандле. Проверено на живом сервере: `.env` пережил полный повторный запуск `install.sh` (в т.ч. с `TLS_PFX_PATH`), Kerberos и аварийный вход остались рабочими.
     Собран и передан новый архив (`vacation-planner-offline-bundle-20260901-222202.tar.gz`, `D:\111`, старые от 01.09.2026 удалены как содержащие оба бага). Инструкция по установке дополнена: шаг про ручную сборку `gssapi` убран (больше не нужен), раздел про Negotiate-политику браузера переписан под реально используемые в организации браузеры — **Yandex Browser** (`AuthServerAllowlist`, тот же движок, что у Chromium) и **Firefox** (`network.negotiate-auth.trusted-uris` / `policies.json`), плюс новый пункт FAQ о том, что `.env` переживает переустановку.
   - ✅ 6.23 Найден и исправлен реальный источник задержек на странице «Оргструктура и сотрудники» (жалоба пользователя: «данные подтягиваются позже») — N+1-запросы, а не нехватка индексов, хотя индексы тоже добавлены попутно. `user_admin_service.list_users_with_roles()` (источник данных `GET /org-units/employees`) вызывал `permissions.resolve_role(db, u)` **в цикле по каждому сотруднику** — при ~3900 сотрудниках это до ~7800 отдельных SQL-запросов на каждую загрузку страницы. Тот же паттерн был и в `org_load_service.get_org_leave_detail()` (дашборд «Отпуска подразделений»). Исправлено: новая `permissions.resolve_roles_bulk(db, users)` — те же 2 запроса независимо от количества пользователей (собирает множества hr_admin/manager разом, затем разбирает роль каждому в Python); оба места переведены на неё, точечный `resolve_role()` для одного пользователя (логин, проверки прав актора и т.п.) не тронут. **Измерено на реальных данных теста (3937 сотрудников)**: `GET /org-units/employees` — 5,38 с → 0,60 с (~9×). Заодно добавлены недостающие индексы на внешние ключи, которые фильтровались в WHERE/CTE без индекса (миграция `a1b2c3d4e5f6`): `org_units.parent_id` (рекурсивные CTE предков/потомков), `org_units.head_user_id` (проверка роли `manager`, теперь и в bulk-версии), `users.org_unit_id` и `leave_requests.user_id` (фильтры дашборда загруженности по юниту/диапазону дат). Применено и проверено на живом тестовом сервере (192.168.0.10) — 196 backend-тестов проходят, оба эндпоинта отдают корректные данные после фикса.
   - ✅ 6.24 Убрана мёртвая настройка «Авторизация» (OIDC issuer/client_id/secret) со страницы «Настройки интеграций» — сохраняла `auth_configuration` в БД, но бэкенд это значение нигде не читает (реальная авторизация с Phase 6.11 целиком в `.env`/`AUTH_PROVIDER`, а не в этой настройке). Компонент `AuthForm` удалён из `IntegrationsSettingsPage.tsx` полностью. Константа `AUTH_CONFIGURATION` и её посев в `seed_dev_data.py` оставлены как есть (безобидные, не мешают) — не трогались, т.к. не просили.
   - ✅ 6.25 Логирование Kerberos-авторизации — на реальном боевом сервере (`corp.amurgpz.ru`, вне тестового `test.local`) вход не работал, а диагностировать было нечем: вся цепочка `dependencies.get_current_user` → `kerberos_provider` не логировала почти ничего, кроме одного случая (ошибка самого GSSAPI-рукопожатия). Остальные исходы — нет заголовка от nginx, некорректная схема авторизации вместо `Negotiate`, логин не сопоставился ни с одним `User.email` — просто улетали клиенту 401/403 без единой строки в `journalctl -u vacation-backend`. Исправлено централизованно: `domain_error_handler` (единая точка обработки всех `DomainError`, `app/core/exceptions.py`) теперь логирует каждый `AuthChallengeError` на `INFO` (ожидаемый шаг обычного SPNEGO-рукопожатия — запрос без тикета) и каждый `ForbiddenError` на `WARNING` (тикет/логин дошли до проверки и были отклонены) — подхватывает уже существующий `message_ru` у исключения, дублировать логи по всем местам в `kerberos_provider.py`/`dependencies.py` не пришлось. Дополнительно: сообщение при неверной auth-схеме теперь показывает, что реально прислал клиент (`kerberos_provider.py`), и добавлен позитивный лог при успешном входе (`resolve_login`: какой логин на какой email смэпился) — без этого не с чем сравнить, действительно ли Kerberos не работает вообще или падает только для части пользователей. `settings.log_level` по умолчанию `INFO` — новые логи будут видны сразу после деплоя без доп. настройки. 196 backend-тестов проходят. **Не задеплоено на реальный `corp.amurgpz.ru`** — нет прямого доступа к этому серверу (в отличие от `test.local`), нужно передать код/бандл и попросить перезапустить `vacation-backend`, затем смотреть `journalctl -u vacation-backend -f` при следующей попытке входа.
   - ✅ 6.26 Фронтенд без формы «Авторизация» и все фиксы Phase 6.22-6.25 задеплоены и пересобраны в офлайн-бандл — до этого правки жили только в репозитории/на живом сервере через ручной scp, а сам бандл на `D:\111` не пересобирался, из-за чего пользователь совершенно справедливо продолжал видеть форму «Авторизация» и подозревал, что оптимизация БД не попадёт в инсталляцию. Пересобран `vacation-planner-offline-bundle-20260902-230101.tar.gz` (заменил все версии от 01-02.09.2026 на `D:\111`) и **проверен полным циклом установки** (не только ручным копированием файлов, как раньше): распаковка → `install.sh` → `alembic upgrade head` доходит до `a1b2c3d4e5f6` (индексы Phase 6.23) → `gssapi` подхватывается из `wheelhouse` → `.env` не тронут → фронтенд без упоминаний `OIDC Issuer` → `/org-units/employees` отвечает 0,6 с. Заодно обновлён (задеплоен на сам тестовый сервер, вне бандла) фронтенд с фиксом раскладки графика (Phase про «Отпуска подразделений», см. выше).
     **Найден повод для дальнейшего расследования жалобы «всё ещё медленно»**: у каждой строки сотрудника в раскрытом дереве «Оргструктура и сотрудники» (`OrgDirectoryPage.tsx`, `EmployeeRowAdmin` → `useBalanceEditor`) свой отдельный `useQuery` на `GET /leave-balances/{user_id}?year=...` — при нажатии «Развернуть всё» это означает **одну отдельную сеть-заявку на КАЖДОГО из ~3900 сотрудников**, а не одним пакетным запросом. Это отдельная, ещё не исправленная проблема (фронтенд-N+1, а не БД) — если жалоба на медленность связана именно с «Развернуть всё», а не с обычной постраничной загрузкой, дело в этом. Не исправлялось в этой сессии — не был уверен, что это та самая жалоба, и не хотел чинить не спросив; нужно подтверждение и отдельный проход (сделать один пакетный эндпоинт балансов по списку `user_id`, как уже сделано для ролей в 6.23).
   - ✅ 6.27 Диагностирована на реальном логе (не гипотеза — прислали фактический `journalctl` с боевого `sr0-pgbackup-01.corp.amurgpz.ru`) и подтверждена причина «авторизация не работает»: **устаревший kvno в keytab**. `gssapi.raw.misc.GSSError: ...kvno 2 enctype aes256-cts found in keytab but cannot decrypt ticket` — ключ в keytab (версия 2, экспортирован 25.08) не совпадает с текущим ключом учётной записи `HTTP/sr0-pgbackup-01` в AD (пароль/ключ был перевыпущен позже) — KDC шифрует тикеты новым ключом, а keytab расшифровывает старым. Решение вне кода — переэкспорт keytab на стороне IT. Разбор бинарного файла keytab (`D:\111\vp_web.keytab`) сделан вручную (Windows `klist` не читает формат keytab — только тикет-кэш), сопутствующая находка — в keytab остались устаревшие DES-энктайпы (des-cbc-crc/md5) наравне с AES/RC4, не критично, но косвенный признак старого инструмента экспорта.
     **Попутно найден и исправлен реальный баг**, обнаруженный по тому же логу: при этой самой ошибке (невозможность расшифровать тикет) бэкенд падал в необработанное исключение (500), а не отдавал аккуратный `403`. Причина — `python-gssapi` для ошибок расшифровки не бросает исключение сразу из `ctx.step()`, а откладывает его до обращения к `ctx.complete`/`ctx.initiator_name` (внутренний `check_last_err`) — а `try/except` в `kerberos_provider.py` оборачивал только сам `step()`. Исправлено: весь обмен (step → complete → initiator_name) теперь под одним `try/except GSSError`. 196 тестов проходят, дополнительно проверено на живом стенде.
     **Заодно из того же скриншота** — синхронизация «Источник дней отпуска и льгот» тоже сломана: внешняя система (`0svb1-1c-01/gppb_hrm_new3`) отвечает `500 Internal Server Error` практически на все табельные номера (3880 из 3880) начиная с 25.08 (до этого синк исправно шёл с 20.08) — это сбой на стороне внешней системы, не в нашем коде. Добавлено логирование тела ответа при ошибке (`VacationDaysClient.fetch`, тот же приём, что в Phase 6.14 для источника недоступных периодов) — раньше в логе было видно только `Server error '500...' for url ...` без содержимого ответа; теперь при следующем сбое будет видно и тело ответа 1С — есть что показать при эскалации на их сторону.
     Собран и передан новый архив (`vacation-planner-offline-bundle-20260904-011457.tar.gz`, `D:\111`) с обоими фиксами, проверен на живом тестовом сервере (SPNEGO 200, аварийный вход 200, бэкенд не падает). **На реальный `corp.amurgpz.ru` этот код ещё не доставлен** — по-прежнему нет доступа к серверу.
   - ✅ 6.28 Реальная обратная связь с пользователя, работающего на бою (роль hr_admin, авторизован через Kerberos) — пять пунктов, все закрыты:
     - **hr_admin не мог подать заявку на отпуск себе** — было намеренное решение Phase 0 (см. комментарий в `HomePage.tsx`), но на практике HR-администратор тоже уходит в отпуск. Вкладки «Мои заявки»/«Новая заявка» теперь видны и hr_admin — ограничений на бэкенде для этой роли не было и раньше (проверено — `leave_request_service.py` роль вообще не проверяет), это было чисто фронтенд-решение. Убран и авто-редирект с «Мои заявки» на «Оргструктура».
     - **Не виден ход синхронизации, "не всегда отражает результат"** — диагностирована реальная причина: у nginx дефолтный `proxy_read_timeout` 60с, а синк на ~3900 сотрудниках (особенно дни отпуска — по одному табельному номеру за раз) может идти дольше; nginx обрывает соединение раньше, чем бэкенд успевает ответить, хотя сам синк на сервере продолжается и завершается успешно уже после того, как клиент увидел ошибку. `proxy_read_timeout`/`proxy_send_timeout` подняты до 300с в обоих nginx-конфигах. Плюс кнопки запуска синхронизации (все три: оргструктура, учебные планы, дни отпуска) теперь показывают «Выполняется…» вместо статичного текста с одной лишь блокировкой — реальный признак «идёт работа», а не только недоступная кнопка.
     - **Автозапуск по расписанию** — реализован полноценно (был в бэклоге с самого начала, `poll_interval_minutes` существовал в настройках, но ничего его не читало). Встроенный планировщик (`app/scheduler.py`, APScheduler) проверяет каждые 60с все три источника (оргструктура, учебные планы, дни отпуска) — если источник включён и задан `poll_interval_minutes` > 0, и с последнего запуска прошло достаточно времени, запускает синк с `trigger_type="scheduled"`. Поле «Интервал автозапуска, мин.» добавлено в формы учебных планов и дней отпуска (у оргструктуры уже было). Самое важное техническое решение: uvicorn работает с `--workers 2` (deploy/redos8/vacation-backend.service) — наивный планировщик в FastAPI startup-хуке запускался бы в ОБОИХ воркерах и дублировал бы синки. Защита — `pg_try_advisory_lock`/`pg_advisory_unlock` (Postgres advisory lock, на SQLite в тестах просто пропускается — единственный процесс, гонки не бывает): в конкретный момент проверку реально выполняет только тот воркер, что взял лок. **Проверено вживую на тестовом сервере**: включил синк оргструктуры с интервалом 1 минута — по логу видно ровно ОДНО срабатывание за тик (не два), синк прошёл успешно; повторный тик минуту спустя — снова без дублирования. Есть защита от "зависшего" running (бэкенд перезапустили посреди синка) — если запись висит в running дольше 30 минут, считается брошенной и разрешается новый запуск. 6 новых тестов на эту логику (`test_scheduler.py`), 202 backend-теста проходят.
       Попутно нашёл и исправил зависимость по цепочке: `BlockedPeriod.created_by` был `NOT NULL`, а синк учебных планов по расписанию создаёт периоды без пользователя-инициатора (`triggered_by=None`) — упало бы IntegrityError на первом же автозапуске. Миграция `b2c3d4e5f6a7` делает колонку nullable (плюс схема/тип на фронте и бэкенде поправлены соответственно).
     - **"Синхронизация дней отпуска нестабильна, нет статуса"** — то же самое (таймаут nginx + теперь есть "Выполняется…" + автозапуск по расписанию вместо ручного клика каждый раз).
     Логика построения клиентов интеграций (`_build_client`) была продублирована в каждом роутере — вынесена в переиспользуемые `build_client()` в соответствующих сервисах (`sync_service`, `study_period_sync_service`, `vacation_days_sync_service`), чтобы планировщик и роутер гарантированно вели себя одинаково, без риска разъехаться.
     Собран и передан новый архив (`vacation-planner-offline-bundle-20260905-124109.tar.gz`, `D:\111`) — включает `apscheduler`/`tzlocal`/`tzdata` в `wheelhouse` (чистые Python-пакеты, ставятся офлайн наравне с остальными зависимостями, не нужен отдельный шаг, как для `gssapi`). Проверен полным циклом установки на тестовом сервере.
   - ✅ 6.29 Два новых бага с реального боя (реальный лог с `sr0-pgbackup-01.corp.amurgpz.ru`, реальные данные — 3975 сотрудников из `drx`):
     - **Синхронизация оргструктуры падала целиком на одном сотруднике** — `UniqueViolation` по `users_email_key`: источник (`drx`) переприсвоил внешний ID существующему человеку (email `vysuchkov@corp.amurgpz.ru` уже был в базе под другим `internal_id` — вероятно, смена табельного номера/логина в 1С). `_get_or_create_mapping` для нового external_id выдавал свежий `internal_id` и слепо вставлял нового `User` с тем же email — падение на уникальности **валило разом весь batch-insert** (SQLAlchemy пакует INSERT'ы по несколько строк), а значит и всю обработку оставшихся ~3900 сотрудников в этом запуске. Хуже того — обработчик ошибки сам падал повторно (`PendingRollbackError` при попытке записать `run.status="failed"` и закоммитить в уже "отравленной" транзакции), так что клиент получал голый 500 вместо чистого статуса ошибки, а `SyncRun` не обновлялся вовсе. Исправлено в `sync_service.py`: (1) реконсиляция по email — если для нового `external_id` пользователя с таким `internal_id` нет, но пользователь с таким **email** уже существует, довязываем маппинг к его настоящему id вместо вставки дубля (с предупреждением в лог); (2) строка `SyncRun` теперь коммитится отдельно ДО основной работы (переживает откат), обработчик ошибки делает `db.rollback()` перед записью статуса — сессия остаётся пригодной, и `run.status="failed"` действительно долетает до БД. 2 новых теста (`test_user_with_changed_external_id_reconciled_by_email`, `test_failed_sync_leaves_session_usable_for_next_run`).
     - **Подать заявку на отпуск не мог вообще никто** — `NotFoundError: Тип отсутствия 'vacation' не настроен`. Причина: строка `LeaveType(code='vacation')` заводилась только в `scripts/seed_dev_data.py` (для локальной разработки), а не в миграции — на боевой БД, поднятой строго через `install.sh`/`alembic upgrade` без прогона dev-сида, таблица `leave_types` оставалась пустой. Ровно та же категория проблемы, что уже чинили для `restriction_settings` в Phase 6 (миграция `3aa8180f408f`), только этот конкретный сид пропустили тогда. Исправлено миграцией `c3d4e5f6a7b8` (`INSERT ... ON CONFLICT DO NOTHING` по `code`, тот же паттерн). Проверено на живом стенде: `POST /leave-requests/drafts` перестал падать на этой ошибке, дошёл до следующей штатной проверки (баланс).
     204 backend-теста проходят (было 202). Собран, проверен полным циклом установки на тестовом сервере и передан новый архив (`vacation-planner-offline-bundle-20260907-193159.tar.gz`, `D:\111`).
   - ✅ 6.30 Четыре пункта обратной связи с реального использования (роль hr_admin, боевые данные):
     - **Синхронизация отменяла деактивацию подразделений/сотрудников** — `sync_service.py` безусловно ставил `org_units.is_active=True` на каждый прогон (источник вообще не отдаёт этот признак для подразделений) и слепо копировал `is_active` источника для сотрудников, даже если HR отключил их вручную по локальной причине. Сделана «липкая» деактивация: подразделения — `is_active` больше не трогается синком вовсе после создания (только ручное поле HR); сотрудники — синк применяет `is_active` источника только пока пользователь ещё активен (реальное увольнение по-прежнему подхватывается сразу), назад из неактивного состояния — только вручную через админку. 3 новых теста, **проверено на живых данных теста**: деактивировал сотрудника вручную → прогнал синк → остался неактивным.
     - **«Все заявки» не показывали ФИО/подразделение, не было поиска/фильтров** — добавлена схема `LeaveRequestAdminOut` (ФИО + подразделение, тот же приём, что уже был у очереди согласования руководителя), в UI — поиск по ФИО, фильтр по статусу и по подразделению.
     - **В журнале изменений не было видно, кто делегировал или менял оргструктуру** — `audit_service.log()` раньше вызывался только из `admin_override` (правка заявок HR). Добавлено логирование в `delegation_service.grant/revoke`, `org_unit_service.create/update/deactivate`, `user_admin_service.create_user/update_user/deactivate_user/grant_role/revoke_role` — везде, где раньше не было ни следа. Заодно оказалось, что даже там, где логирование уже было, столбца «Кто» в самой таблице журнала попросту не было (только сырой `performed_by` UUID в API, не показанный на странице) — добавлено `performed_by_name` (резолвится на бэкенде, как и ФИО в списке заявок) и колонка в `AuditLogPage.tsx`. **Проверено на живых данных**: деактивация сотрудника корректно легла в журнал с именем исполнителя, а не только UUID.
     - **Заявку руководителя структурного подразделения мог согласовать он сам** — каскад видимости руководителя (`org_unit_service.visible_unit_ids`) включает его собственное подразделение, поэтому `_is_manager_of` тривиально пропускал `reviewer == target` для любого, кто сам возглавляет юнит. Вышестоящий руководитель и раньше корректно мог согласовать (тот же каскад), не так была закрыта только лазейка самосогласования. Точечный фикс: `_is_manager_of` явно запрещает `reviewer.id == target.id`, кроме одного исключения — **самый верхний уровень оргструктуры** (подразделение без родителя — по факту замгендиректора и приравненные, выше согласовывать некого), для них старое поведение сохранено по прямому требованию пользователя («не распространяется на уровень заместителя генерального директора»). Заодно скрыта из очереди согласования руководителя его собственная заявка (иначе кнопка «Согласовать» была бы на виду, но нерабочей — ForbiddenError при клике). 5 новых тестов, покрывающих обе ветки (обычный руководитель / исключение для корневого уровня) и видимость в очереди.
     212 backend-тестов проходят (было 207). Собран, проверен полным циклом установки на тестовом сервере и передан новый архив (`vacation-planner-offline-bundle-20260916-202912.tar.gz`, `D:\111`) — заодно обнаружилось и восстановлено: все предыдущие архивы с `D:\111` и локального `dist/` пропали (папку, видимо, почистили между сессиями) — `python-standalone.tar.gz`/`wheelhouse` (с уже собранными `gssapi`/`apscheduler`) пришлось забирать обратно с тестового сервера, где остались распакованные копии прошлых установок.
   - ✅ 6.31 Тестовое подключение для синхронизации учебных планов (недоступные периоды) — жалоба: «синхронизация сбоит, непонятно почему», история синка при сбое показывала только «ошибок запроса: N» без деталей. Новый эндпоинт `POST /admin/study-periods/test` (HR-admin) — 1–5 табельных номеров, реквизиты берутся прямо из полей формы (необязательно уже сохранённых — можно проверить новый URL/логин до сохранения), **ничего не пишет в БД** (ни `SyncRun`, ни `BlockedPeriod` — проверено на живом стенде: до/после теста число строк в `sync_runs` не изменилось). На каждый номер возвращает: полностью сформированный URL запроса (с `Period1`/`Period2`/`Employee`, как реально уйдёт), HTTP-статус, превью тела ответа и либо количество распознанных записей, либо точную причину сбоя (не удалось соединиться / HTTP-ошибка / невалидный JSON / формат ответа не совпал с ожидаемым — раньше все четыре ситуации в UI выглядели одинаково как «синхронизация не прошла»). В `IntegrationsSettingsPage` — блок «Тестовое подключение» в форме источника учебных планов (только режим HTTP): поле для номеров через запятую/пробел, кнопка «Проверить», результат по каждому номеру с деталями и разворачиваемым телом ответа. 5 новых тестов на `StudyPeriodsClient.test_fetch` (успех, HTTP-ошибка, обрыв соединения, невалидный JSON, неожиданный формат ответа) — 217 backend-тестов проходят. Проверено на живом стенде реальным запросом к боевому URL источника (`0svb1-1c-01.corp.amurgpz.ru`) — корректно показал точную причину сбоя (DNS не резолвится из изолированной тестовой сети, что и ожидалось) вместе со сформированным запросом. Собран и передан новый архив (`vacation-planner-offline-bundle-20260916-204135.tar.gz`, `D:\111`), проверен полным циклом установки.
   - ✅ 6.32 Терминология: «доплата» → «выплата ЕСВ» везде в интерфейсе и сообщениях (бонус к длинному отпуску, `bonus_requested`) — «Все заявки», очередь согласования, форма новой заявки (чекбокс и ошибка), настройка-переключатель в «Ограничениях». Само поле/API `bonus_requested`/`vacation_bonus` не переименовывались (внутренний идентификатор, не пользовательский текст) — меняли только видимые подписи и текст ошибок валидации. 217 тестов проходят (текст ошибок нигде не проверялся дословно). Собран и передан новый архив (`vacation-planner-offline-bundle-20260916-211205.tar.gz`, `D:\111\Grafik`).
   - ✅ 6.33 Синхронизация недоступных периодов (учебные планы) не разбирала реальный ответ источника — тестовое подключение из Phase 6.31 сразу же, при первом реальном использовании, окупилось: пользователь прислал настоящий пример ответа боевого источника, и в нём нашлось сразу три расхождения с форматом, заложенным в `app/integrations/study_periods.py` изначально (по предварительному, не до конца согласованному контракту):
     - **Дата** — источник отдаёт ISO 8601 (`"2027-01-01T00:00:00"`), код ожидал `"24.10.2024 0:00:00"` (`strptime("%d.%m.%Y %H:%M:%S")`) — падало с `ValueError`.
     - **Опечатка в названии поля** — ключ в реальном ответе `"ПрограмаОбучения"` (одна «м»), код читал `"ПрограммаОбучения"` (две «м») — падало с `KeyError`.
     - **Структура ответа** — режим HTTP (запрос по одному табельному номеру за раз) отдаёт ПЛОСКИЙ список записей без обёртки по табельному номеру (сам номер в ответе не приходит вовсе, только в запросе), а `parse_entries` ожидал структуру файлового режима — блоки, сгруппированные по табельному номеру. Из-за этого `run_http_sync` попросту не мог сопоставить пришедшие записи ни с одним сотрудником.
     Исправлено: `_parse_date` сначала пробует ISO 8601 (`datetime.fromisoformat`), при неудаче — старый формат с точками (для режима "файл", где формат не подтверждён окончательно); чтение названия программы сначала пробует `"ПрограмаОбучения"` (подтверждённое написание), затем `"ПрограммаОбучения"` (fallback); добавлена `parse_flat_entries(employee_code, raw)` для плоского формата режима HTTP — `run_http_sync` теперь разбирает ответ сразу после `fetch_raw()` для каждого сотрудника (пока табельный номер ещё в контексте), а не склеивает сырые данные всех сотрудников в один список для разбора в конце; сбой разбора одного сотрудника (например, источник опять поменял формат) теперь фиксируется как персональная ошибка этого сотрудника, не роняя весь прогон синхронизации. `StudyPeriodsClient.test_fetch` (тестовое подключение из Phase 6.31) переведён на `parse_flat_entries` — раньше он тоже не мог правильно разобрать реальный ответ. 4 новых теста (2 в `test_study_periods_client.py` — реальный формат, ISO-дата, одна «м»; 2 в `test_study_period_sync_service.py` — `run_http_sync` на реальном формате и на персональной ошибке разбора одного сотрудника), 3 старых теста обновлены под подтверждённый формат вместо предварительного. 219 backend-тестов проходят (было 217). Проверено на реальном примере ответа, присланном пользователем (5 записей, все даты/часы разобраны верно). Развёрнуто на тестовом сервере (192.168.0.10), собран и передан новый архив (`vacation-planner-offline-bundle-20260917-190420.tar.gz`, `D:\111\Grafik`).
   - ✅ 6.34 Три пункта обратной связи по графику отпусков подразделений (`OrgLoadDashboardPage`):
     - **Кнопка «Согласовать» на собственной заявке руководителя** — на этом отдельном графике (в отличие от очереди согласования, где то же самое уже чинилось в Phase 6.30) кнопки согласования в подсказке по дню показывались независимо от того, чья это заявка: `canApprove` проверял только роль, не смотрел `l.user_id === currentUser.id`. Кнопка была видна, но нажатие на своей же заявке всё равно получало `ForbiddenError` от `approval_service.approve` (self-approval там запрещён с Phase 6.30) — то есть повторилась та же категория «нерабочей кнопки», что уже исправляли. Добавлена та же проверка `selfApprovalExempt` (руководитель без вышестоящего — корневое подразделение), что и на бэкенде: кнопки скрываются для собственной заявки, кроме этого исключения.
     - **На графике не видно, чьи заявки согласованы, а чьи нет, без клика по дню** — двойное решение по итогам обратной связи: (1) у каждой ячейки дня добавлена рамка — пунктирная янтарная, если среди отпусков этого дня (в текущей области анализа) есть хоть одна несогласованная заявка, иначе прозрачная; подсказка (title) при наведении тоже говорит «все согласованы» / «есть несогласованные»; (2) по прямому пожеланию пользователя («предлагаю сразу выводить их справа, не ждать клика») панель справа сделана всегда видимой, а не только после клика по дню — по умолчанию показывает список ВСЕХ несогласованных заявок текущей области анализа (ФИО, даты, статус, кнопки согласования), отсортированный по дате начала; клик по дню переключает панель на список за конкретный день, крестик возвращает к общему списку несогласованных.
     - **Не видно, когда конкретный сотрудник уходит в отпуск (все его периоды разом)** — по клику на ФИО сотрудника (в списке слева или в правой панели) все его периоды на всём графике (за весь год, без учёта текущих фильтров по роли/поиску/ручному выбору) обводятся синей рамкой; повторный клик снимает подсветку. Заголовок над списком слева показывает, кто сейчас подсвечен, с кнопкой «Убрать подсветку». Обновлена легенда графика (добавлены обозначения новой рамки и пунктира).
     Чисто фронтенд — все нужные данные (`status` каждой заявки, полный список отпусков подразделения) уже приходили с бэкенда, новых эндпоинтов не потребовалось. `npx tsc -b` проходит без ошибок.
     Визуальная проверка в браузере: встроенный browser-инструмент этой сессии сначала не годился (облачная песочница без доступа к внутренней сети — внешние адреса открываются, 192.168.0.10 нет); после установки расширения Claude in Chrome пользователем удалось подключиться к его реальному Chrome (в его сети) и проверить вживую на test.ru с реальными данными (роль HR Admin Fallback, подразделение «Служба информационно-управляющих систем», реальная заявка Пономарева Р.С. на 2 периода). Подтверждено: список справа показывается сразу без клика, пунктирная рамка появляется на днях с несогласованными заявками, синяя рамка корректно обводит именно периоды выбранного сотрудника (и не задевает чужие).
     **Тут же пользователь поймал ещё один баг живьём**: панель «На согласовании» показывала два отдельных пункта с двумя парами кнопок для ОДНОЙ заявки Пономарева (два периода, 01.02–19.02 и 01.03–27.03) — нарушение принципа «1 человек — 1 заявка», уже соблюдаемого в `ApprovalQueuePage` (там периоды одной заявки группируются по `submission_id` и согласуются одним действием). Причина: новая панель строилась по плоскому списку `leavesInScope` без группировки. Исправлено — та же группировка по `submission_id`, что и в очереди согласования: один пункт на заявку (сотрудник + все её периоды списком), одна пара кнопок, `handleApproveSubmission`/`handleRejectSubmission` вызываются с первым периодом группы (бэкенд `_get_requests_for_submission` и так согласует/отклоняет все периоды с тем же `submission_id` одной транзакцией — фронтенд просто была неправильно ЭТО показывал). Проверено повторно на том же тестовом стенде: «На согласовании (1)» вместо «(2)», оба периода Пономарева — под одной записью с одной парой кнопок.
     Заодно при живой проверке подтверждён ещё один, отдельный от этой задачи баг (не исправлялся в рамках Phase 6.34, оставлен на решение отдельно): кнопка «Согласовать» на этом графике показывается всем с ролью `manager`/`hr_admin` (`canApprove`), но бэкенд (`approval_service._is_manager_of`) разрешает согласование только реальному руководителю по иерархии — HR-admin, не являющийся линейным руководителем конкретного подразделения, получает 403 при попытке нажать кнопку, которая ему показана. Та же категория «нерабочей кнопки», что уже чинилась для самосогласования, но для роли hr_admin.
     Собран и передан новый архив (`vacation-planner-offline-bundle-20260917-212548.tar.gz`, `D:\111\Grafik`).
   - ✅ 6.35 Крупный пакет обратной связи (10 пунктов) с реального использования:
     1. **Недоступные периоды при пересечении показывали только 1 из них** — `blocked_period_rule.py` брал только `blocks[0]` для текста ошибки (хотя `params.blocked_periods` уже содержал все). Теперь в тексте перечислены все пересекающиеся периоды. Новый тест.
     2. **Учебный курс блокировал только 1 день независимо от продолжительности** — `КолВоЧасов` из 1С игнорировалось, `study_period_sync_service._apply` всегда ставил `date_to = date_from`. Добавлена `_end_date()` — длительность в днях = `ceil(часы / 8)`, обновление существующего периода теперь тоже реагирует на изменение продолжительности, а не только текста программы. 2 новых теста.
     3. **HR-admin, одновременно возглавляющий подразделение, не видел вкладку «Согласование»** — `role` вычисляется с приоритетом (`resolve_role`: hr_admin перекрывает manager), поэтому `isManager` на фронтенде было `false` для такого пользователя, хотя по факту у него есть прямые подчинённые. Добавлен независимый от `role` флаг `is_org_unit_head` (`CurrentUserOut`, `permissions.is_org_unit_head`) — вкладка показывается при `isManager || is_org_unit_head`.
     4. **Недоступные периоды фактически мог менять только HR/админ** — `blocked_period_service._check_can_manage` сверял буквальное совпадение `head_user_id` с org_unit_id/org_unit сотрудника, без каскада вниз по иерархии (в отличие от `approval_service`/`org_unit_service.visible_unit_ids`, где вышестоящий руководитель управляет и нижестоящими отделами). На практике это делало форму рабочей только для HR. Переведено на `visible_unit_ids` — тот же каскад, что и везде в приложении. Новый тест на управление подчинённым отделом вышестоящим руководителем.
     3b. Добавлены поиск (по сотруднику/причине) и фильтр по области действия на странице «Недоступные периоды» — раньше был только фильтр по году.
     5. **В «Ограничениях» были английские названия параметров** — `study_periods_source`/`vacation_days_source` не исключались из общего списка (в отличие от `external_source_connection`/`auth_configuration`, у которых своя форма на вкладке «Интеграции»), поэтому попадали в таблицу ограничений с сырым английским ключом вместо названия и без полей для реального редактирования (у них там нет обработчика параметров) — дублирующий и вводящий в заблуждение чекбокс «вкл». Оба ключа добавлены в исключения — у них уже есть полноценные формы на «Интеграциях».
     6. **У HR-admin в «Все заявки» не было отмены** — добавлена кнопка «Отменить» (кроме уже отменённых), использует существующий `admin_override(status="cancelled")` с обязательной причиной через `window.prompt`.
     7. **Фильтр по подразделению в «Все заявки» не учитывал вложенные отделы** — добавлен переключатель «с вложенными подразделениями»: раскрывает выбранный юнит до всех потомков по `parent_id` на клиенте (список подразделений уже загружен), без нового запроса к бэкенду.
     8. **В журнале изменений не было ни поиска/фильтра, ни разбора идентификаторов, ни чистки** — добавлены: поиск (кто/действие/причина), фильтр по типу сущности, фильтр по датам; значения полей «Было»/«Стало», совпадающие с id известного подразделения/сотрудника, теперь показывают имя вместо голого UUID (сопоставление на фронтенде по уже загруженным спискам `/org-units`, `/org-units/employees`); колонка «Сущность» показывает тип + имя. Очистка журнала (`audit_service.clear`, `DELETE /admin/audit-log?date_from=&date_to=`) — за произвольный период или целиком, с подтверждением. Аналогичная чистка истории добавлена и для всех трёх видов синхронизации (`sync_service.clear_history`, `DELETE /admin/{sync,study-periods,vacation-days}/runs`) — отдельный пункт (4) в исходной заявке пользователя («дать возможность чистить историю — лог»). Новые тесты на `audit_service.clear` и `sync_service.clear_history`.
     9. **Дата приёма/увольнения — новое поле, ограничение на ЕСВ по стажу**. `User.hire_date`/`termination_date` (миграция `d4e5f6a7b8c9`) синкаются из ТОГО ЖЕ источника, что и дни отпуска (`GetVacationDaysCount`, см. `app/integrations/vacation_days.py`) — ожидаемый контракт (источник пока не отдаёт эти поля, добавляются на стороне 1С по этой сессии):
        ```json
        [{"Employee": "...", "Podr": "...", "Position": "...",
          "DaysCount": 36, "L": "Нет", "Kat": "Специалисты",
          "HireDate": "01.09.2027", "DismissalDate": null}]
        ```
        `DismissalDate` — `null`/отсутствует/пустая строка, пока сотрудник работает. Парсер дат нестрогий (сначала ISO 8601, затем `ДД.ММ.ГГГГ`) — та же защита, что и после истории с датами study_periods (Phase 6.33). Пустое значение поля не затирает уже известную дату (как и `employee_code` в `sync_service`).
        Ограничение по стажу (после уточнения у пользователя на конкретном примере — принят 01.09.2027, когда впервые доступна ЕСВ к отпуску 20 дней → ответ «не раньше 01.07.2028», т.е. `hire_date + 10 месяцев`): для сотрудников со стажем **менее года** выплата ЕСВ недоступна раньше `hire_date + N месяцев` (по умолчанию 10, настраивается). При стаже год и более ограничение не действует вовсе. Реализовано отдельным переключаемым ограничением `vacation_bonus_tenure_restriction` (миграция `e5f6a7b8c9d0`, `enabled=False` по умолчанию — включать после проверки, что `hire_date` реально приходит из 1С), UI на странице «Ограничения». Без `hire_date` у сотрудника ограничение просто не применяется (не блокирует «на всякий случай»). 8 новых тестов (парсинг дат, синк, эффект ограничения, включая независимость от общего порога `vacation_bonus.min_days`).
        **Вторая часть правила («для остальных») уточнена и реализована отдельным туром вопросов** — оказалось, что «остальные» это сотрудники со стажем ГОД И БОЛЬШЕ (не «периоды ≤13 дней», как я предполагал сначала), и правило не одноразовое, а ЕЖЕГОДНО повторяющееся, завязанное на плановый год: месяц приёма минус сдвиг (по умолчанию 6 мес.) даёт месяц планового года, начиная с которого доступна ЕСВ; принятым в 1-й половине года ограничения нет вовсе (расчётная дата уже в прошлом). Подтверждено пользователем тремя точными примерами (принят 12.07.2021 → доступно с 12.01 планового года; принят 12.12.2021 → с 12.06; принят в июне или раньше → без ограничений) — реализованная `_veteran_cutoff()` даёт те же три даты. Второй параметр настройки — `veteran_shift_months` (по умолчанию 6), независимый от `new_hire_months` (10, переименован из `months` с обратной совместимостью) для правила новичков. UI на «Ограничениях» показывает оба поля с пояснением. 3 новых теста на примерах пользователя (242 backend-теста, было 239).
     Развёрнуто на тестовом сервере (192.168.0.10) включая миграции, собран и передан новый архив (`vacation-planner-offline-bundle-20260918-202221.tar.gz`, `D:\111\Grafik`).
     **Требует отдельного уточнения от пользователя** (заявлено как «новый случай, воспроизвёл только что», но конкретные шаги воспроизведения ещё не получены): пункт 3 исходного списка — «синхронизация подразделений и сотрудников ломает активность». Код липкой деактивации (Phase 6.30) на месте и не изменился; нужен точный сценарий воспроизведения на актуальном стенде, чтобы не чинить вслепую то, что не воспроизводится.
   - ✅ 6.36 Переверстка всего интерфейса в корпоративном синем стиле («Вариант B — рабочий кабинет», выбран пользователем из эскизов на артефакте после нескольких раундов правок — усиление различий A/B, затем выбор B). Весь существующий функционал сохранён как есть, менялась только вёрстка/классы:
     - Общая тема (`frontend/src/index.css`) — переменные для светлой/тёмной темы (`prefers-color-scheme`; ручной переключатель добавлен позже, см. 6.37), гарнитуры PT Sans / PT Sans Narrow (Google Fonts), переиспользуемые классы: `.btn-primary/.btn-ghost/.btn-outline`, `.panel`, `.card`/`.cards`, `.badge` (ok/wait/bad/neutral), `table.t`, `.toolbar`, `.fieldrow`/`.field`, `.settingrow`, `.hint`/`.error-text`/`.empty`, каркас боковой панели `.app-shell/.app-rail/.app-main`.
     - `HomePage.tsx` — верхняя вкладочная навигация заменена на левую боковую панель с иконками (`frontend/src/navIcons.tsx`, простые инлайн-SVG без иконочных шрифтов) и группировкой: базовые пункты, «Подразделение», «Администрирование» — та же логика видимости вкладок по ролям (`isManagerOrHr`/`isHrAdmin`/`canApproveOwnTeam`), только перегруппирована для сайдбара.
     - Все 12 страниц (`MyRequestsPage`, `RequestFormPage`, `ApprovalQueuePage`, `BlockedPeriodsPage`, `OrgDirectoryPage`, `OrgLoadDashboardPage`, `DelegationsPage`, `SyncPage`, `RestrictionSettingsPage`, `IntegrationsSettingsPage`, `AllRequestsPage`, `AuditLogPage`) и `DevLoginPage` переведены на общие классы вместо разрозненных инлайн-стилей; статусные бейджи заявок унифицированы через новый `statusBadgeClass()` в `statusLabel.ts`. `OrgDirectoryPage.tsx` (самый сложный компонент — рекурсивное дерево подразделений) сделан консервативно: только замена хардкод-цветов на CSS-переменные (для тёмной темы) без переписывания структуры разметки — риск сломать вложенную логику редактирования себя не оправдывал. Семантические цвета загруженности (`bandColor` — зелёный/жёлтый/красный) в `OrgLoadDashboardPage` оставлены как есть — это не акцентный, а сигнальный цвет, независимо от темы.
     `npx tsc -b` и `vite build` проходят чисто. Развёрнуто на тестовом сервере (192.168.0.10) — подтверждено `curl`, шрифт/CSS грузятся (200), тёмная тема применяется (проверено через JS eval на странице: фон `#0b1116` соответствует токену). **Визуально в браузере пройти дальше страницы логина не удалось** — на этом стенде Chrome виснет (не за ~10 c, как раньше, а на неопределённое время, до 40+ c без ответа) на Kerberos Negotiate-запросе к `/api/users/me` при заходе по IP вместо hostname (`test.ru` не резолвится на этой машине через DNS) — окружающая проблема, не связанная с этой правкой: тот же самый хендшейк подвисал и до переверстки. Подтверждено через `fetch()` из консоли страницы (с таймаутом и корректными fallback-заголовками HR-admin) — запрос не долетает до сервера, `curl` к тому же адресу отвечает за 27 мс. Собран новый архив — **доставка отложена**: диск `D:` временно недоступен на рабочей станции пользователя (виден только `C:`/`E:`), архив лежит в `deploy/redos8/dist/vacation-planner-offline-bundle-20260919-144600.tar.gz`, будет скопирован в `D:\111\Grafik\` как только диск переподключат.
   - ✅ 6.37 Раунд правок по живому тестированию после переверстки (2026-09-19/20; всё выложено на тестовый стенд, бандл `vacation-planner-offline-bundle-20260920-203349.tar.gz` в `D:I\Grafik`):
     - **ЕСВ по стажу разделён на две независимые настройки** (`vacation_bonus_new_hire_restriction`, `vacation_bonus_veteran_restriction`) — миграция `f6a7b8c9d0e1` переносит старую строку с прежними значениями (применяется в `install.sh`, шаг [5/7]). Сравнение с отсечкой теперь идёт с **датой начала планируемого периода**, а не с `date.today()` (план составляют заранее); тесты переписаны на фиксированные даты.
     - **Праздничные дни внутри отпуска не списываются с лимита** (конец периода не двигается): `backend/app/core/holidays.py` (ст. 112 ТК + 31.12) — используется в остатке баланса, проверке «хватает дней», минимальной длительности, пороге ЕСВ, `LeaveRequest.days`; на фронтенде `holidays.ts` (`countLeaveDays`) и подпись «праздничных N не считаются» в форме заявки. Переносы выходных по постановлениям не учитываются.
     - **«Все заявки» (HR):** периоды одной заявки — в одной строке; «Отменить заявку» отменяет все периоды (`whole_submission` в `admin-override`, запись в журнале на каждый период), «Изменить» — по одному периоду, в форме правки добавлена галочка выплаты ЕСВ.
     - **«Мои заявки»** показывают и заявки, поданные по делегированию за подопечных (колонка «Сотрудник»).
     - **Оргструктура:** колонки «Приём»/«Увольнение», даты редактируются в форме «изменить» (`UserUpdate.hire_date/termination_date`); там же поля «дней начислено»/«перенесено» (инлайн-правка баланса в таблице убрана — теперь только показ); в «Отпуска подразделений» руководитель юнита всегда попадает в список сотрудников, даже если административно числится выше; пустые `()` у подразделений без типа убраны.
     - **Интерфейс:** ручной переключатель темы; календарь выбора периода — отдельные токены для «выходной/недоступно/в плане» (различимы в обеих темах); блок пользователя в боковой панели больше не «уезжает» на длинных страницах (sticky); `color: inherit` для всех `<button>` (стрелки дерева были невидимы в тёмной теме); календарь загрузки подразделений — фиксированная карточка без переноса под фильтр; кнопка «Сохранить» в «Ограничениях» выделяется при несохранённых изменениях; журнал изменений — русские названия полей и значений; ошибка согласования на графике загрузки показывается окном, а не теряется; русские статусы (draft и др.).
     - Тесты бэкенда: 249 passed. Выкладка на стенд — по SSH под root, паролем от пользователя (в план не пишется).
   - ⬜ Открыто после 6.37: (а) у HR-админа нет права согласовывать заявки чужих отделов (только руководитель по цепочке) — решить, нужно ли; (б) жалоба «отмена задевает всю заявку» воспроизводилась только как ожидаемое поведение «Моих заявок»/«Согласования» — теперь то же и в «Все заявки»; (в) баланс дней «не сохранялся» — код проверен, причина не найдена, редактирование перенесено в форму; (г) делегирование: на тесте работает (заявку за Пономарева подали от test2), на боевом проверить; (д) проверка безопасности итогового набора правок не проводилась.
   - ⬜ Playwright smoke-тест как часть репозитория (пока проверки делаются вручную на реальном Postgres в песочнице, не закоммичены)
   - ⬜ Категория сотрудника (`kat`: руководитель/специалист/рабочий) из `GetEmployeers` — в текущей реализации не сохраняется (роль руководителя уже вычисляется через `head_user_id`, назначение остальных явно не запрашивалось)

## Открытые допущения

- Есть ли у внешней системы гарантированно один `head_user_id` на юнит, или нужен локальный override — подтверждено реальными данными (`GetDepartments`), полей достаточно, override не потребовался.
- Категория сотрудника (`kat`) из `GetEmployeers` не используется — уточнить у пользователя, нужна ли (сейчас роль "руководитель" полностью выводится из `org_units.head_user_id`).
- `romprs/work` — отдельный, не связанный с проектом репозиторий; писать туда для этого проекта не нужно (ошибочно использовался один раз из-за автоматически подставленного репозитория в рабочей среде, исправлено после уточнения пользователя).
- **Аутентификация стенда:** на 24.09.2026 Kerberos challenge включён, dev-вход недоступен. Прежнее ожидание keytab не описывает текущее HTTP-поведение стенда. Готовность доменного входа конкретного продуктивного сервера проверяется отдельно с его hostname/keytab; текущая сверка не проверяла эти реквизиты.

- **Синхронизация недоступных периодов:** прежняя запись о недиагностированном сбое относится к Phase 6.14. Позже добавлены тестовое подключение (6.31) и разбор фактического формата ответа (6.33). Текущая доступность источника и успешный боевой синк 24.09.2026 не проверялись; для проверки используйте тестовое подключение и журнал синхронизаций.
- **Офлайн-бандл для РЕД ОС 8** (`deploy/redos8/dist/`) — не в git (`.gitignore`), пересобирается вручную командой `powershell -File deploy\redos8\build-bundle.ps1 -PythonStandaloneUrl "<релиз python-build-standalone>"` из корня репозитория. Нужно останавливать локальный dev-сервер фронтенда перед сборкой — `npm ci` иначе падает `EPERM` на файл `esbuild.exe`, занятый работающим `vite`. Пересобирается по запросу пользователя (архив от даты сборки быстро устаревает относительно кода — свежие коммиты в него не попадают автоматически).
